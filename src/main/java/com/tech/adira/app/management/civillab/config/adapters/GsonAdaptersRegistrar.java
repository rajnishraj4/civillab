package com.tech.adira.app.management.civillab.config.adapters;

import com.google.gson.GsonBuilder;
import de.huxhorn.sulky.ulid.ULID;

public class GsonAdaptersRegistrar {
    public static void registerAll(GsonBuilder builder) {
//        builder.registerTypeAdapter(ULID.Value.class, new UlidGsonAdapter());
    }
}
